apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  generateName: kill-recommendations-grpc-
  namespace: nutrition
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: recommendations-grpc
  duration: "30s"